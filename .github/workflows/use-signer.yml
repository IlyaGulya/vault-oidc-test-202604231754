name: Call central signer (from repo A)

on:
  pull_request:
    branches: [main]

jobs:
  invoke-sign:
    uses: IlyaGulya/vault-oidc-central-202604231804/.github/workflows/signer.yml@main
    permissions:
      contents: read
      id-token: write
    with:
      caller_note: "from-caller1"
